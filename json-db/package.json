{
  "name": "json-db",
  "version": "1.0.0",
  "description": "Mocked Database build with Json Server",
  "private": true,
  "scripts": {
    "start": "json-server db.json --middlewares file.js --port 3004",
    "post-update": "echo \"codesandbox preview only, need an update\" && yarn upgrade --latest"
  }
}